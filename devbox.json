{
  "packages": [
    "nodejs@latest",
    "bun@latest",
    "rye@latest"
  ],
  "shell": {
    "init_hook": [
      "echo 'Welcome to devbox!' > /dev/null"
    ],
    "scripts": {
      "test": [
        "echo \"Error: no test specified\" && exit 1"
      ],
      "backend": "rye run litestar run --debug --reload --host 0.0.0.0",
      "frontend": [
        "cd front",
        "bun install",
        "npm run dev -- --host"
      ]
    }
  }
}